<p id="notice"><%= bootstrap_flash %></p>
<div class="page-header">
<h1>Agenda</h1>
</div>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Inicio da Reserva</th>
      <th>Fim da Reserva</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @reservas.each do |reserva| %>
      <tr>
        <td><%= reserva.horarioinicio %></td>
        <td><%= reserva.horariofim %></td>
        <td><%= link_to 'Ver', reserva %></td>
        <td><%= link_to 'Editar', edit_reserva_path(reserva) %></td>
        <td><%= link_to 'Destruir', reserva, method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Criar Reserva', new_reserva_path %>
